<div class ="form-new mt-4">
<h1>Create a new category</h1>
<%= simple_form_for [@category] do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Submit", class: 'btn btn-warning' %>
<% end %>
</div>
